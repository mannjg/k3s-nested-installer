apiVersion: v1
kind: Service
metadata:
  name: k3s-nodeport
  namespace: k3s-inner
  labels:
    app: k3s
spec:
  type: NodePort
  selector:
    app: k3s
  ports:
  - name: api
    port: 6443
    targetPort: 6443
    protocol: TCP
    nodePort: 30443
